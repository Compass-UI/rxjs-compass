import { MarbleBeadsDirective } from './marble-beads.directive';

describe('MarbleBeadsDirective', () => {
  it('should create an instance', () => {
    const directive = new MarbleBeadsDirective();
    expect(directive).toBeTruthy();
  });
});
